   <div class="row justify-content-center">
                <!-- Single Rooms Area -->
                <div class="col-12 col-md-6 col-lg-5">
                    <div class="single-rooms-area wow fadeInUp" data-wow-delay="100ms">
                        {% for image in gallery %}
                        <!-- Thumbnail -->
                        <div class="bg-thumbnail bg-img" style="background-image: url({{images.image_url.url}});">
                        <img src="{{image.image_url.url}}">
                        </div>
                        
                        <!-- Price -->
                        <p class="price-from">{{image.image_name}}</p>
                        <!-- Rooms Text -->
                        <div class="rooms-text">
                            <div class="line"></div>
                            <h4>{{image.image_name}}</h4>
                            <p>{{image.image_desc}}</p>
                        </div>
                        <!-- Book Room -->
                        <!-- <a href="/rooms" class="book-room-btn btn lukenyaGetaway-btn">Book Room</a> -->
                    </div>
                    {% endfor %}
                </div>
    
                <!-- Single Rooms Area -->
              
    
            </div>